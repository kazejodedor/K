import requests
import time

TOKEN = "MTM4NjMzMDM3NTk1Mjc5MzcyMw.GqCzJF.qeXQ4M1SaknJ6qntKLc21qr_GlzGSoX_xmxs_w"

# ← PON AQUÍ EL ID DEL GRUPO DM (Group Chat)
GROUP_CHANNEL_ID = "1482162306237599835"   # ejemplo, cámbialo por el real

# Lista de nombres que quieres poner (puedes agregar cuantos quieras)
nombres = [
    "Grupo de las perras asociales 😂",
    "Mami Eduarda + Warszlady limpio ano",
    "JAJAJAJA DOMAORRA XD",
    "Server de los Undeanos Dioses 🔥",
    "P9 ROFL TU NALGA",
    "Anti Difamadoras Official",
    "Cum Dump Group"
]

veces = 9999999999          # prácticamente infinito
intervalo = 1.5             # segundos entre cambios (ajusta si quieres más rápido)

URL = f"https://discord.com/api/v9/channels/{GROUP_CHANNEL_ID}"

headers = {
    "Authorization": TOKEN,
    "Content-Type": "application/json",
    "User-Agent": "Mozilla/5.0"   # ayuda un poco a no ser tan obvio
}

print("Iniciando spam de cambio de nombre del grupo...")

for i in range(veces):
    nuevo_nombre = nombres[i % len(nombres)]   # rota entre los nombres

    data = {"name": nuevo_nombre}

    try:
        r = requests.patch(URL, headers=headers, json=data)

        if r.status_code in (200, 204):
            print(f"✅ {i+1} | Nombre cambiado a: {nuevo_nombre}")
        else:
            print(f"❌ Error {r.status_code} - Intento {i+1}")
            print(r.text[:300])

            if r.status_code == 429:   # rate limit
                retry = r.json().get("retry_after", 5)
                print(f"Rate limit → esperando {retry + 3} segundos")
                time.sleep(retry + 3)
                continue

            if r.status_code == 401:
                print("Token inválido o revocado. Terminando.")
                break

            if r.status_code == 403:
                print("No tienes permiso o el grupo ya no existe.")
                break

    except Exception as e:
        print(f"Excepción: {e}")
        time.sleep(5)

    time.sleep(intervalo)

print("Script terminado.")
